# Hister

Hister is a self-hosted search engine for your browser history, visited pages, local files, and personal knowledge base. It keeps the index on your own server and provides a web UI for private full-text search.

This Runtipi app uses the official upstream container image and stores Hister data in `/hister/data` with SQLite by default.
